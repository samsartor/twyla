//! twyla — a typst-based static site generator.
//!
//! Currently a thin facade exposing the `diff` module used by the
//! porting-verification CLI and (eventually) the build pipeline.

pub mod diff;
